# Test Fixtures

JSON document-model fixtures used by the CLI test harness (`python -m engine.cli <fixture>`).

Added in Milestone 2:
- `minimal.json` — title, one author, abstract, a few paragraphs
- `wide_eq.json` — wide figures, wide tables, math equations
- `edge_cases.json` — very long captions, many references, nested lists
